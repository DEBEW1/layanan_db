name: pengaduan_masyarakat
description: Aplikasi Layanan Pengaduan Masyarakat.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  http: ^1.2.1
  flutter_secure_storage: ^9.2.2
  image_picker: ^1.1.2
  intl: any
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true